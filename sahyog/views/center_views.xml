<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- List View -->
    <record id="view_center_tree" model="ir.ui.view">
        <field name="name">sahyog.center.list</field>
        <field name="model">sahyog.center</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_center_form" model="ir.ui.view">
        <field name="name">sahyog.center.form</field>
        <field name="model">sahyog.center</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_center" model="ir.actions.act_window">
        <field name="name">Centers</field>
        <field name="res_model">sahyog.center</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
