<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- CSV Import Wizard Form View -->
    <record id="view_csv_import_wizard_form" model="ir.ui.view">
        <field name="name">sahyog.csv.import.wizard.form</field>
        <field name="model">sahyog.csv.import.wizard</field>
        <field name="arch" type="xml">
            <form string="Import Program Schedules from CSV">
                <group>
                    <field name="csv_file" filename="csv_filename"/>
                    <field name="csv_filename" invisible="1"/>
                </group>
                <separator string="Expected CSV Format"/>
                <p>
                    The CSV file must contain the following columns in order:<br/>
                    <b>Program Name, Start Date, End Date, Start Time, End Time, Status, Location, Capacity, Fee, Notes</b><br/><br/>
                    Dates should be in YYYY-MM-DD format. Status values: Planning, Upcoming, Completed.<br/>
                    Program Name must match an existing program (case-insensitive).
                </p>
                <footer>
                    <button name="action_import" string="Import" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Action to open the wizard -->
    <record id="action_csv_import_wizard" model="ir.actions.act_window">
        <field name="name">Import Schedules from CSV</field>
        <field name="res_model">sahyog.csv.import.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
